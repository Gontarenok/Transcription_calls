[Unit]
Description=Enqueue pending KC classification
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/transcription-calls
ExecStart=/usr/bin/docker compose --env-file .env --env-file .image.env run --rm web python scripts/enqueue.py classify --limit 200
