[Unit]
Description=Enqueue KC daily pipeline
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/transcription-calls
EnvironmentFile=/opt/transcription-calls/prod.env
ExecStart=/usr/bin/docker compose run --rm web python scripts/enqueue.py kc_day

