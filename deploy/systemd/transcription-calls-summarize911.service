[Unit]
Description=Enqueue pending 911 summarization
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/transcription-calls
ExecStart=/usr/bin/docker compose --env-file .env --env-file .image.env run --rm web python scripts/enqueue.py summarize_911 --limit 100
